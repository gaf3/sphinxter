.. created by sphinxter
.. default-domain:: py

sphinxter.Writer
================

.. currentmodule:: sphinxter

.. class:: Writer(document, file)

    Class for writing out documents

    :param document:
    :param file:

    .. attribute:: document

    .. attribute:: file

    .. method:: attribute(parsed, indent)

        :param parsed:
        :param indent:

    .. method:: attributes(parsed, indent)

        :param parsed:
        :param indent:

    .. method:: cls(parsed, indent=0)

        :param parsed:
        :param indent:

    .. method:: definition(parsed, indent)

        :param parsed:
        :param indent:

    .. method:: description(parsed, indent)

        :param parsed:
        :param indent:

    .. method:: dump()

    .. method:: execution(parsed, indent)

        :param parsed:
        :param indent:

    .. method:: function(parsed, indent=0)

        :param parsed:
        :param indent:

    .. method:: line(line='', indent=0, before=False, after=False)

        :param line:
        :param indent:
        :param before:
        :param after:

    .. method:: lines(lines, indent, before=False, after=False)

        :param lines:
        :param indent:
        :param before:
        :param after:

    .. method:: method(parsed, indent)

        :param parsed:
        :param indent:

    .. method:: module(parsed, indent=0)

        :param parsed:
        :param indent:

    .. method:: parameter(parsed, indent)

        :param parsed:
        :param indent:

    .. method:: parameters(parsed, indent)

        :param parsed:
        :param indent:

    .. method:: raises(parsed, indent)

        :param parsed:
        :param indent:

    .. method:: returns(parsed, indent)

        :param parsed:
        :param indent:

    .. method:: toctree(paths, indent=0)

        :param paths:
        :param indent:

    .. staticmethod:: types(types)

        :param types:

    .. method:: usage(parsed, indent)

        :param parsed:
        :param indent:
